
	Extension de las clases de persistencia

		-----------------------------------------------------------
		-- RELACIONES
		-----------------------------------------------------------
	
		1) objeto_datos_relacion
		
			Especializacion del comportamiento de la relacion entre datos.
 				- Capturar eventos de tablas para mantener la integridad de las relaciones
				- Validaciones sobre el conjunto de datos
 
					function evt__validar()									- Antes de sincronizar, despues de las tablas
	**				function evt__[tabla]__pre_agregar()					- En linea con la manipulacion de tablas
	**				function evt__[tabla]__post_agregar()					- En linea con la manipulacion de tablas
	**				function evt__[tabla]__pre_modificar()					- En linea con la manipulacion de tablas
	**				function evt__[tabla]__post_modificar()					- En linea con la manipulacion de tablas
	**				function evt__[tabla]__pre_eliminar()					- En linea con la manipulacion de tablas
	**				function evt__[tabla]__post_eliminar()					- En linea con la manipulacion de tablas

		
		-----------------------------------------------------------

		3) ap_relacion_db

			** Especializacion del proceso de persitencia en una DB.

				- Control de la integridad del modelo de datos
				- Definir reglas de actualizacion de datos
				- Logs a nivel proceso
			
					protected function evt__pre_eliminacion(){}			- Antes de sincronizar
					protected function evt__post_eliminacion(){}		- Despues de sincronizar		
					protected function evt__pre_sincronizacion(){}		- Antes de sincronizar
					protected function evt__post_sincronizacion(){}		- Despues de sincronizar		

		
		-----------------------------------------------------------
		-- TABLAS
		-----------------------------------------------------------

		3) objeto_datos_tabla
		
			** Especializacion del comportamiento de la tabla.
				(Presisar el sentido de la misma como unidad dentro de la logica de dominio)
				- Agregar validaciones sobre las filas

					function evt__validar_ingreso($fila, $id=null)		- En linea con el ingreso de datos
					function evt__validar_fila($fila)					- Validacion "global"


		-----------------------------------------------------------

		4) ap_tabla_db_s
		
			** Especializacion del proceso de persitencia en una DB

				- Control de la integridad del modelo de datos
				- Definir reglas de actualizacion de datos
				- Logs a nivel tablas

					protected function evt__pre_sincronizacion(){}		- Antes de sincronizar
					protected function evt__post_sincronizacion(){}		- Despues de sincronizar
					protected function evt__pre_insert($id){}			- Antes de ejecutar un INSERT sobre la tabla
					protected function evt__post_insert($id){}			- Despues de ejecutar un INSERT sobre la tabla
					protected function evt__pre_update($id){}			- Antes de ejecutar un UPDATE sobre la tabla   
					protected function evt__post_update($id){}          - Despues de ejecutar un UPDATE sobre la tabla 
					protected function evt__pre_delete($id){}			- Antes de ejecutar un DELETE sobre la tabla   
					protected function evt__post_delete($id){}          - Despues de ejecutar un DELETE sobre la tabla
					
				- Reescribir el SQL

					protected function generar_sql_insert($id)	- Mapeo de un registro a la DB		
					protected function generar_sql_delete($id)	- Mapeo de un registro a la DB
					protected function generar_sql_update($id)	- Mapeo de un registro a la DB
					protected function generar_sql_select($id)	- Carga de un registro
														
				- Definicion de carga de columnas "externas"
			
					(carga externa)
						definir $this->activar_proceso_carga_externa_dao(... parametros...)
						ó $this->activar_proceso_carga_externa_sql(... parametros...)
						en: function inicializar(){}

		-----------------------------------------------------------			